[project]
name = "llm-model-serving"
version = "0.1.0"
description = "Model export utilities for Triton"
requires-python = ">=3.10"

dependencies = [
    "torch>=2.2.0",
    "onnx>=1.15.0",
    "onnxscript>=0.6.2",
]

[tool.setuptools]
py-modules = []